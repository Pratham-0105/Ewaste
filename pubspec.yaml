name: waste_management_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.6.5
  image_picker: ^1.0.7
  random_string: ^2.3.1
  shared_preferences: ^2.2.2
  google_generative_ai: ^0.4.7
  pdf: ^3.10.7
  printing: ^5.11.0
  url_launcher: ^6.3.2
  # Missing packages that caused your errors:
  curved_navigation_bar: ^1.0.6
  google_sign_in: ^6.2.1
  geolocator: ^13.0.1
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "images/Icon/app_icon.png"